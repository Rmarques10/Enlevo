@{
    ViewData["Title"] = "Home Page";
}


    <div class="item">
        <img src="~/images/bn-15.png" alt="Visual Studio" class="img-responsive" />      
    </div>
      

    <html>
    <head>
        <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
        <script type="text/javascript">
            google.charts.load('current', { 'packages': ['corechart'] });
            google.charts.setOnLoadCallback(drawChart);

            function drawChart() {

                var data = google.visualization.arrayToDataTable([
                    ['Task', 'Hours per Day'],
                    ['Corte De Cabelo', @ViewBag.corte ],
                    ['Escova', @ViewBag.barba],
                    ['Pedicure', @ViewBag.manicure],
                    ['Manicure', @ViewBag.pedicure],
                    ['Barba', @ViewBag.escova]
                ]);

                var options = {
                    title: 'Serviços Mais Utilizados'
                };

                var chart = new google.visualization.PieChart(document.getElementById('piechart'));

                chart.draw(data, options);
            }

        </script>
    </head>
    <body>
        <div id="piechart" style="width: 900px; height: 500px;"></div>
    </body>
    </html>